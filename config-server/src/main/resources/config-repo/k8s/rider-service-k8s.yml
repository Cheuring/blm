server:
  port: 8085

spring:
  datasource:
    url: jdbc:mysql://mysql-service:3306/microservices?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver
    
# Eureka配置 - K8S环境
eureka:
  instance:
    hostname: rider-service
    prefer-ip-address: false
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    service-url:
      defaultZone: http://eureka-service:8761/eureka/
