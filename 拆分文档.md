好的，这是一个将您的单体外卖项目重构为微服务架构的详细拆分文档。

## 微服务拆分文档：饱了么外卖平台

### 1\. 总体架构

我们将遵循领域驱动设计 (DDD) 的原则，按业务边界将单体应用拆分为多个高内聚、低耦合的微服务。

**核心服务:**

  * **用户服务 (user-service):** 负责用户注册、登录、信息管理、地址管理等。
  * **商家服务 (store-service):** 负责店铺、商品、商品分类、促销活动等管理。
  * **订单服务 (order-service):** 负责订单的创建、状态流转、支付、评价等。
  * **骑手服务 (rider-service):** 负责骑手注册、接单、位置更新、订单配送等。
  * **管理后台服务 (admin-service):** 负责平台的管理功能，如用户、商家、骑手的审核与管理，以及数据统计等。

**支撑服务:**

  * **API 网关 (api-gateway):** 作为所有请求的入口，负责路由、认证、限流等。
  * **认证中心 (auth-center):** 统一处理用户的认证和授权，发放 JWT。
  * **服务注册与发现 (discovery-service):** 使用 Nacos 或 Eureka，让服务间可以互相发现。
  * **配置中心 (config-center):** 使用 Nacos 或 Spring Cloud Config，集中管理所有服务的配置。
  * **文件服务 (file-service):** 统一处理文件上传下载，对接MinIO。

### 2\. 微服务详解

#### 2.1 用户服务 (user-service)

**职责:** 核心用户管理。

**代码结构:**

```
user-service/
├── src/main/java/com/blm/user/
│   ├── UserApplication.java
│   ├── controller/
│   │   ├── UserController.java         // 用户个人信息管理
│   │   └── UserAddressController.java  // 用户地址管理
│   ├── service/
│   │   ├── impl/
│   │   │   └── UserServiceImpl.java
│   │   ├── UserService.java
│   │   └── UserAddressService.java
│   ├── entity/
│   │   ├── User.java
│   │   └── UserAddress.java
│   ├── repository/
│   │   ├── UserRepository.java
│   │   └── UserAddressRepository.java
│   └── dto/...
└── src/main/resources/
    ├── application.yml
    └── mapper/...
```

**文件说明及接口定义:**

  * **`UserController.java`**:
      * `GET /profile`: 获取当前用户信息。
      * `PUT /profile`: 更新当前用户信息。
      * `PUT /password`: 修改密码。
  * **`UserAddressController.java`**:
      * `GET /addresses`: 获取地址列表。
      * `POST /addresses`: 添加地址。
      * `PUT /addresses/{id}`: 更新地址。
      * `DELETE /addresses/{id}`: 删除地址。
      * `POST /addresses/{id}/default`: 设为默认地址。

#### 2.2 商家服务 (store-service)

**职责:** 店铺和商品信息管理。

**代码结构:**

```
store-service/
├── src/main/java/com/blm/store/
│   ├── StoreApplication.java
│   ├── controller/
│   │   ├── StoreController.java
│   │   ├── FoodController.java
│   │   ├── FoodCategoryController.java
│   │   └── PromotionController.java
│   ├── service/
│   │   ├── impl/...
│   │   ├── StoreService.java
│   │   ├── FoodService.java
│   │   ├── FoodCategoryService.java
│   │   └── PromotionService.java
│   ├── entity/...
│   ├── repository/...
│   └── dto/...
└── src/main/resources/...
```

**文件说明及接口定义:**

  * **`StoreController.java`**:
      * `POST /`: 创建店铺。
      * `GET /`: 获取商家的店铺信息。
      * `PUT /{id}`: 更新店铺信息。
      * `PUT /{id}/status`: 更改店铺状态。
  * **`FoodController.java`**:
      * `GET /{storeId}/foods`: 获取商品列表。
      * `POST /{storeId}/foods`: 添加商品。
  * **`FoodCategoryController.java`**:
      * `GET /{storeId}/categories`: 获取商品分类列表。
  * **`PromotionController.java`**:
      * `GET /{storeId}/promotions`: 获取促销列表。

#### 2.3 订单服务 (order-service)

**职责:** 订单全生命周期管理。

**代码结构:**

```
order-service/
├── src/main/java/com/blm/order/
│   ├── OrderApplication.java
│   ├── controller/
│   │   ├── OrderController.java
│   │   └── CartController.java
│   ├── service/
│   │   ├── impl/...
│   │   ├── OrderService.java
│   │   └── CartService.java
│   ├── entity/...
│   ├── repository/...
│   ├── dto/...
│   └── client/
│       ├── UserClient.java      // Feign客户端，调用用户服务
│       └── StoreClient.java     // Feign客户端，调用商家服务
└── src/main/resources/...
```

**文件说明及接口定义:**

  * **`OrderController.java`**:
      * `POST /`: 创建订单。
      * `GET /`: 获取订单列表。
      * `GET /{id}`: 获取订单详情。
      * `PUT /{id}/cancel`: 取消订单。
  * **`CartController.java`**:
      * `GET /cart`: 获取购物车。
      * `POST /cart`: 添加商品到购物车。
  * **`UserClient.java` (Feign 接口)**:
      * `getUserAddress(@PathVariable("id") Long id)`: 获取用户地址。
  * **`StoreClient.java` (Feign 接口)**:
      * `getStoreById(@PathVariable("id") Long id)`: 获取店铺信息。
      * `getFoodById(@PathVariable("id") Long id)`: 获取商品信息。

#### 2.4 骑手服务 (rider-service)

**职责:** 骑手相关业务。

**代码结构:**

```
rider-service/
├── src/main/java/com/blm/rider/
│   ├── RiderApplication.java
│   ├── controller/
│   │   └── RiderController.java
│   ├── service/...
│   ├── entity/...
│   ├── repository/...
│   ├── dto/...
│   └── client/
│       └── OrderClient.java     // Feign客户端，调用订单服务
└── src/main/resources/...
```

**文件说明及接口定义:**

  * **`RiderController.java`**:
      * `POST /register`: 注册成为骑手。
      * `PUT /status`: 更新工作状态。
      * `PUT /location`: 更新骑手位置。
  * **`OrderClient.java` (Feign 接口)**:
      * `getAvailableOrders()`: 获取可接订单。
      * `acceptOrder(@PathVariable("orderId") Long orderId)`: 骑手接单。

#### 2.5 管理后台服务 (admin-service)

**职责:** 平台管理功能。

**代码结构:**

```
admin-service/
├── src/main/java/com/blm/admin/
│   ├── AdminApplication.java
│   ├── controller/
│   │   ├── AdminUserController.java
│   │   ├── AdminStoreController.java
│   │   └── AdminRiderController.java
│   ├── service/...
│   ├── client/
│   │   ├── UserClient.java
│   │   ├── StoreClient.java
│   │   └── RiderClient.java
│   └── vo/...
└── src/main/resources/...
```

**文件说明及接口定义:**

  * **`AdminUserController.java`**:
      * `GET /users`: 获取用户列表。
      * `PUT /users/{id}/status`: 更改用户状态。
  * **`AdminStoreController.java`**:
      * `GET /stores`: 获取商家列表。
      * `PUT /stores/{id}/status`: 审核商家店铺。
  * **`AdminRiderController.java`**:
      * `GET /riders`: 获取骑手列表。
      * `PUT /riders/{id}/status`: 更改骑手状态。

### 3\. 服务间通信

我们将使用 **OpenFeign** 进行服务间的同步调用。

**示例：`order-service` 调用 `user-service`**

在 `order-service` 中定义 Feign 客户端:

```java
// com/blm/order/client/UserClient.java
@FeignClient("user-service")
public interface UserClient {

    @GetMapping("/api/addresses/{id}")
    Result<UserAddressVO> getUserAddress(@PathVariable("id") Long id);
}
```

在 `order-service` 的 `pom.xml` 中添加 OpenFeign 依赖:

```xml
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-openfeign</artifactId>
</dependency>
```

在 `OrderApplication.java` 上添加 `@EnableFeignClients` 注解。

### 4\. 数据库设计

每个微服务将拥有自己独立的数据库，以实现数据的隔离。

  * `user_db`: 包含 `user` 和 `user_address` 表。
  * `store_db`: 包含 `store`, `food`, `food_category`, `promotion` 等表。
  * `order_db`: 包含 `orders`, `order_detail`, `cart` 等表。
  * `rider_db`: 包含 `rider`, `rider_stats` 等表。

### 5\. 部署架构

建议使用 Docker 和 Kubernetes 进行容器化部署和管理，配合 Jenkins 等工具实现 CI/CD 自动化部署。

希望这份详细的拆分文档对您有所帮助！